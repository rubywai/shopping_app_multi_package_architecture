name: cart
description: "Local cart package for shopping app"
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^8.2.0
  sqflite: ^2.4.2
  path: ^1.9.0
  flutter_riverpod: ^3.0.3
  go_router: ^16.3.0
  common:
    path: ../common

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: false

